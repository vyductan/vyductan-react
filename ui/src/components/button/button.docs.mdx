# Button Component Documentation

## Overview

A versatile button component that combines Ant Design's API with Shadcn UI's styling system. Supports multiple variants, sizes, states, and composition patterns.

## Import

```tsx
import { Button } from "@/components/ui/button";
```

## Basic Usage

```tsx
<Button>Default Button</Button>
<Button variant="primary">Primary Button</Button>
<Button variant="outline">Outline Button</Button>
<Button variant="text">Ghost Button</Button>
```

### Color & Variant

You can set the `color` and `variant` attributes at the same time can derive more variant buttons.

```tsx
<Flex vertical gap="small">
  <Flex gap="small" wrap>
    <Button color="default" variant="solid">
      Solid
    </Button>
    <Button color="default" variant="outlined">
      Outlined
    </Button>
    <Button color="default" variant="dashed">
      Dashed
    </Button>
    <Button color="default" variant="filled">
      Filled
    </Button>
    <Button color="default" variant="text">
      Text
    </Button>
    <Button color="default" variant="link">
      Link
    </Button>
  </Flex>
  <Flex gap="small" wrap>
    <Button color="primary" variant="solid">
      Solid
    </Button>
    <Button color="primary" variant="outlined">
      Outlined
    </Button>
    <Button color="primary" variant="dashed">
      Dashed
    </Button>
    <Button color="primary" variant="filled">
      Filled
    </Button>
    <Button color="primary" variant="text">
      Text
    </Button>
    <Button color="primary" variant="link">
      Link
    </Button>
  </Flex>
  <Flex gap="small" wrap>
    <Button color="danger" variant="solid">
      Solid
    </Button>
    <Button color="danger" variant="outlined">
      Outlined
    </Button>
    <Button color="danger" variant="dashed">
      Dashed
    </Button>
    <Button color="danger" variant="filled">
      Filled
    </Button>
    <Button color="danger" variant="text">
      Text
    </Button>
    <Button color="danger" variant="link">
      Link
    </Button>
  </Flex>
  <Flex gap="small" wrap>
    <Button color="pink" variant="solid">
      Solid
    </Button>
    <Button color="pink" variant="outlined">
      Outlined
    </Button>
    <Button color="pink" variant="dashed">
      Dashed
    </Button>
    <Button color="pink" variant="filled">
      Filled
    </Button>
    <Button color="pink" variant="text">
      Text
    </Button>
    <Button color="pink" variant="link">
      Link
    </Button>
  </Flex>
  <Flex gap="small" wrap>
    <Button color="purple" variant="solid">
      Solid
    </Button>
    <Button color="purple" variant="outlined">
      Outlined
    </Button>
    <Button color="purple" variant="dashed">
      Dashed
    </Button>
    <Button color="purple" variant="filled">
      Filled
    </Button>
    <Button color="purple" variant="text">
      Text
    </Button>
    <Button color="purple" variant="link">
      Link
    </Button>
  </Flex>
  <Flex gap="small" wrap>
    <Button color="cyan" variant="solid">
      Solid
    </Button>
    <Button color="cyan" variant="outlined">
      Outlined
    </Button>
    <Button color="cyan" variant="dashed">
      Dashed
    </Button>
    <Button color="cyan" variant="filled">
      Filled
    </Button>
    <Button color="cyan" variant="text">
      Text
    </Button>
    <Button color="cyan" variant="link">
      Link
    </Button>
  </Flex>
</Flex>
```

### Icon

You can add an icon using the icon property.

```tsx
import { Icon } from "@acme/ui/icons";

<Button icon={<Icon icon="icon-[lucide--plus]" />}>Add Item</Button>;
```

## API Reference

### Props

| Prop         | Type                                                            | Default     | Description                  |
| ------------ | --------------------------------------------------------------- | ----------- | ---------------------------- |
| `variant`    | `"default" \| "outline" \| "text" \| "link" \| "destructive"`   | `"default"` | Button visual variant        |
| `size`       | `"default" \| "small" \| "large" \| "icon" \| "middle" \| "sm"` | `"middle"`  | Button size                  |
| `color`      | `"default" \| "danger" \| "success" \| "link"`                  | `"default"` | Button color theme           |
| `shape`      | `"default" \| "circle" \| "round"`                              | `"default"` | Button shape                 |
| `type`       | `HtmlType \| ButtonType`                                        | `"default"` | Button type (HTML or custom) |
| `htmlType`   | `"button" \| "submit" \| "reset"`                               | `"button"`  | HTML button type             |
| `loading`    | `boolean`                                                       | `false`     | Show loading spinner         |
| `disabled`   | `boolean`                                                       | `false`     | Disable button               |
| `icon`       | `React.ReactNode`                                               | -           | Icon element                 |
| `href`       | `string`                                                        | -           | Make button act as link      |
| `asChild`    | `boolean`                                                       | `false`     | Use Slot for composition     |
| `primary`    | `boolean`                                                       | -           | Primary styling (legacy)     |
| `srOnly`     | `boolean`                                                       | `false`     | Screen reader only text      |
| `classNames` | `object`                                                        | -           | Custom class names           |

## Examples

### Variants

```tsx
<Button variant="default">Default</Button>
<Button variant="primary">Primary</Button>
<Button variant="outline">Outline</Button>
<Button variant="text">Ghost</Button>
<Button variant="link">Link</Button>
<Button variant="destructive">Destructive</Button>
```

### Sizes

```tsx
<Button size="small">Small</Button>
<Button size="sm">Small (alias)</Button>
<Button size="middle">Middle</Button>
<Button size="default">Default</Button>
<Button size="large">Large</Button>
<Button size="icon">
  <Icon icon="icon-[lucide--plus]" />
</Button>
```

### Shapes

```tsx
<Button shape="default">Default</Button>
<Button shape="round">Round</Button>
<Button shape="circle" size="icon">
  <Icon icon="icon-[lucide--plus]" />
</Button>
```

### Loading State

```tsx
<Button loading>Loading...</Button>
<Button loading disabled>
  Processing
</Button>
```

### As Link

```tsx
<Button href="/dashboard">Go to Dashboard</Button>
<Button href="https://example.com" variant="link">
  External Link
</Button>
```

### Composition with asChild

```tsx
<Button asChild>
  <a href="/profile">
    <Icon icon="icon-[lucide--user]" />
    Profile
  </a>
</Button>
```

### Form Integration

```tsx
<form>
  <Button htmlType="submit" variant="primary">
    Submit Form
  </Button>
  <Button htmlType="reset" variant="outline">
    Reset
  </Button>
</form>
```

### Screen Reader Only Text

```tsx
<Button srOnly size="icon">
  Settings
  <Icon icon="icon-[lucide--settings]" />
</Button>
```

### Custom Styling

```tsx
<Button
  classNames={{
    variants: {
      // Custom Tailwind Variants configuration
    },
  }}
  className="custom-button-class"
>
  Custom Button
</Button>
```

## Advanced Usage

### Button Groups (Future)

```tsx
// Coming soon
<Button.Group>
  <Button>Left</Button>
  <Button>Middle</Button>
  <Button>Right</Button>
</Button.Group>
```

### With Tooltips

```tsx
import { Tooltip } from "@/components/ui/tooltip";

<Tooltip content="Click to save">
  <Button size="icon">
    <Icon icon="icon-[lucide--save]" />
  </Button>
</Tooltip>;
```

## Accessibility

- Supports all standard button ARIA attributes
- Proper focus management and keyboard navigation
- Screen reader friendly with `srOnly` prop
- Loading state announcements
- Disabled state handling

## Styling System

The button uses Tailwind Variants for consistent styling:

- Base styles for common properties
- Variant-based conditional styling
- Responsive design support
- Dark mode compatibility
- Custom theme integration

## Type Safety

Full TypeScript support with:

- Strict prop typing
- Variant inference
- Composition pattern types
- HTML attribute inheritance

## Performance

- Optimized re-renders with React.forwardRef
- Lazy icon loading support
- Minimal bundle impact
- Efficient class name generation

## Browser Support

- Modern browsers (Chrome, Firefox, Safari, Edge)
- Mobile browsers
- Accessibility tools compatibility

## Migration Notes

### From Ant Design Button

- Most props are compatible
- `type` prop behavior slightly different
- Custom styling approach changed

### From Shadcn Button

- Enhanced API with more variants
- Additional size options
- Better composition support
