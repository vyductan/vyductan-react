import { ComponentSource } from "@acme/ui/components/mdx";

import DateRangePickerBasicDemo from "./demo/range-basic";
import DateRangePickerVariantsDemo from "./demo/range-variants";

# DateRangePicker

A date range picker component that allows users to select a range of dates. Built on top of Dayjs and follows Ant Design API patterns with Shadcn UI styling.

## Examples

### Basic Usage

Display the document sample code for the basic usage of the DateRangePicker component.

<ComponentSource
  src="date-picker/demo/range-basic.tsx"
  __comp__={DateRangePickerBasicDemo}
/>

### Variants

DateRangePicker supports different variants, sizes, and configurations to display different visual styles and behaviors.

<ComponentSource
  src="date-picker/demo/range-variants.tsx"
  __comp__={DateRangePickerVariantsDemo}
/>
