# Shadcn Integration

This directory contains shadcn integration/reference code.

## Protected path rules

- NEVER edit files under this directory by default.
- Treat this directory as read-only reference code.
- If a task requires changes here, stop and ask for explicit confirmation first.
