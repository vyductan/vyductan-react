import { Meta } from "@storybook/addon-docs/blocks";

import { ComponentSource } from "@acme/ui/components/mdx";

import OptionLabelPropDemo from "./option-label-prop";

<Meta isTemplate />

### Option Label Prop

Use `optionLabelProp` to pick which option field renders in the trigger after selection, while the dropdown keeps its full `label`. Open the menu to see the long names, then note the short label in the trigger.

<ComponentSource
  src="auto-complete/examples/option-label-prop.tsx"
  __comp__={OptionLabelPropDemo}
/>
