---
title: Form
description: A form component for data collection and validation.
component: true
---

import { ComponentSource } from "@acme/ui/components/mdx";

import AntdDemo from "./examples/antd";
import BasicDemo from "./examples/basic";
import LayoutMultipleDemo from "./examples/layout-multiple";
import ShadcnDemo from "./examples/shadcn";

# Form

A form component for data collection and validation.

## Features

- 📋 **Schema Validation** - Built-in support for Zod schema validation
- 🧩 **Type Safety** - Full TypeScript integration with `react-hook-form`
- 🎨 **Flexible Layout** - Support for horizontal, vertical, and inline layouts
- 🔌 **Ant Design Compatible** - Supports `labelCol`, `wrapperCol`, and other Ant Design props
- ♿ **Accessible** - Built on top of Radix UI primitives

> [!IMPORTANT]
> **Prioritize Shadcn Usage**
>
> We strongly recommend using the **Shadcn** style (via `zod` and `useForm`) for all new forms. This ensures better type safety, performance, and consistency with the rest of the UI.
>
> The Ant Design style (using `Form.Item` with `rules`) is supported for migration purposes but should be avoided for new development.

## Examples

### Basic Usage (Shadcn Style)

The recommended way to build forms using `zod` schema and `useForm`.

<ComponentSource src="form/examples/basic.tsx" __comp__={BasicDemo} />

### Complex Form (Shadcn Style)

A complex form example demonstrating various form fields and validation.

<ComponentSource src="form/examples/shadcn.tsx" __comp__={ShadcnDemo} />

### Layout Control

Control label layout using `labelCol` and `wrapperCol`, similar to Ant Design. This is useful for horizontal forms.

<ComponentSource
  src="form/examples/layout-multiple.tsx"
  __comp__={LayoutMultipleDemo}
/>

### Ant Design Style (Legacy)

> [!WARNING]
> This style is supported for backward compatibility. Please use the Shadcn style for new forms.

<ComponentSource src="form/examples/antd.tsx" __comp__={AntdDemo} />

## API Reference

### Form Props

| Property     | Type                                     | Default      | Description                          |
| ------------ | ---------------------------------------- | ------------ | ------------------------------------ |
| `form`       | `UseFormReturn`                          | -            | The form instance from `useForm`     |
| `layout`     | `"horizontal" \| "vertical" \| "inline"` | `"vertical"` | Form layout                          |
| `labelCol`   | `ColProps`                               | -            | Label layout configuration           |
| `wrapperCol` | `ColProps`                               | -            | Control wrapper layout configuration |
| `labelAlign` | `"left" \| "right"`                      | `"right"`    | Label text alignment                 |
| `labelWrap`  | `boolean`                                | `false`      | Whether label can wrap               |
| `colon`      | `boolean`                                | `true`       | Whether to show colon after label    |
| `disabled`   | `boolean`                                | `false`      | Whether to disable all form fields   |

### Field Props

| Property      | Type        | Default | Description                                    |
| ------------- | ----------- | ------- | ---------------------------------------------- |
| `control`     | `Control`   | -       | The control object from `useForm`              |
| `name`        | `string`    | -       | The field name                                 |
| `label`       | `ReactNode` | -       | The field label                                |
| `description` | `ReactNode` | -       | The field description                          |
| `tooltip`     | `ReactNode` | -       | Tooltip text or node                           |
| `required`    | `boolean`   | `false` | Whether the field is required (shows asterisk) |

### useForm

We use `react-hook-form` under the hood. See [react-hook-form documentation](https://react-hook-form.com/docs/useform) for full API details.
