import { ComponentSource } from "@acme/ui/components/mdx";

import SearchFilterDemo from "./search-filter";

### Search & Filter

Provide custom filter logic when matching needs to account for accents or domain-specific rules.

<ComponentSource src="auto-complete/examples/search-filter.tsx" __comp__={SearchFilterDemo} />
