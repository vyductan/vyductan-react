chore: add vyductan-react baseline workspace

Capture the current workspace contents as the initial baseline for the newly initialized vyductan-react repository so future changes can be tracked incrementally.
